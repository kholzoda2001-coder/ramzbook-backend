const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const c = await prisma.course.findFirst({ where: { targetLanguage: { code: 'ru' }, nativeLanguage: { code: 'tg' } }, include: { modules: { include: { lessons: { include: { words: true } } } } } }); if (!c) { console.log('Course not found'); return; } console.log(JSON.stringify(c, null, 2)); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
