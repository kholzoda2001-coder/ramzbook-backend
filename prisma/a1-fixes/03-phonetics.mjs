// ─────────────────────────────────────────────────────────────────────────────
// 03 — Талаффузи тоҷикӣ (`ipaTajik`)
//
// СЕ МАСЪАЛА:
//
//  1. `th` (/θ/, /ð/) ҳамчун «с» ё «з» навишта шудааст — 28 корт.
//     Худи барнома дар `utils/grammar_notes.dart` менависад:
//       «th (think, this): забонро БАЙНИ дандонҳо гузоред —
//        НА «с/з», НА «т/д»».
//     Яъне мазмун бо дастури худи барнома зид аст.
//
//  2. `w` ҳамчун «в» навишта шудааст — 43 корт. Ҳамон варақа мегӯяд:
//       «w (water, west): лабҳоро МУДАВВАР кунед (мисли «у»)»
//     ва базаи худ аллакай дар ду ҷо дуруст навиштааст:
//       `Weather` = «уэзер» · `Wind` = «уинд»
//
//  3. Номҳои хос ва вожаҳои байналмилалӣ талаффузи РУСӢ доранд, на англисӣ:
//       Address → «адрес» · Passport → «паспорт» · Manager → «менеҷер»
//
// ⚠️ ЧАРО ҶАДВАЛИ ДАСТӢ, НА ҚОИДАИ ХУДКОР.
//    Қоидаи содда «ҳар «с»-ро дар калимаи th-дор иваз кун» калимаи `Thirsty`
//    (/ˈθɜː.sti/ → «тёрсти»)-ро вайрон мекунад: «с»-и он ҷо садои ВОҚЕИИ /s/
//    аст, на /θ/. Ҳамин тавр `Thermometer`, `Healthy` ва ғайра. Пас ҳар
//    сатр дастӣ навишта шудааст.
//
// ⚠️ ҚАРОРИ ТАЪЛИМӢ, КИ ТАСДИҚ МЕХОҲАД: садои `th` дар алифбои тоҷикӣ ҲАРФ
//    НАДОРАД. Се роҳ буд:
//      (a) «с/з»          — ҳозира; худи барнома онро манъ мекунад
//      (b) `th`-и лотинӣ   — аниқ, вале хати омехта дар миёни калима
//      (c) «тҳ» / «дҳ»     — ҳарфҳои тоҷикӣ, ҳаракати забон+нафасро ишора
//                            мекунад ва ба тавзеҳи худи варақа мувофиқ аст
//    Ин ҷо (c) интихоб шуд. Иваз кардан — ду сатри поён.
// ─────────────────────────────────────────────────────────────────────────────
import { connect, loadCourse, loc, Changes } from './_lib.mjs';

/** ⚙️ Ин ду сатрро иваз кунед, агар шакли дигари `th` қабул шавад. */
export const TH_VOICELESS = 'тҳ'; // think, three, mouth   (/θ/)
export const TH_VOICED    = 'дҳ'; // this, father, weather (/ð/)

const th  = (s) => s.replaceAll('◇', TH_VOICELESS).replaceAll('◆', TH_VOICED);

/**
 * Калид — калимаи АНГЛИСӢ (ҳарфи хурд, тавре ки дар база аст).
 * Қимат — талаффузи НАВИ тоҷикӣ. `◇` = /θ/, `◆` = /ð/ (аз боло иваз мешаванд).
 *
 * Ҷадвал ҳамзамон масъалаи «як калима — ду талаффуз»-ро ҳал мекунад
 * (Father: «фазэр» ↔ «фаазер»), чунки калид калимаи англисист, на сатри дарс.
 */
const IPA_TJ = {
  // ── /θ/ бесадо ───────────────────────────────────────────────────────────
  'thank you':    '◇энк ю',
  'three':        '◇рии',
  'thirteen':     '◇ёртин',
  'thirty':       '◇ёрти',
  'thursday':     '◇ёрздэй',
  'thirsty':      '◇ёрсти',      // «с»-и дуюм воқеан /s/ аст — нигоҳ дошта шуд
  'thermometer':  '◇эмомитэ',
  'mouth':        'мау◇',
  'tooth':        'туу◇',
  'toothache':    'туу◇эйк',
  'healthy':      'ҳэл◇и',
  'earth':        'ёр◇',
  'birthday':     'бёр◇дэй',
  'bathroom':     'баа◇рум',
  'sore throat':  'сор ◇роут',

  // ── /ð/ бо садо ──────────────────────────────────────────────────────────
  'father':       'фаа◆э',
  'mother':       'ма◆э',
  'brother':      'бра◆э',
  'grandfather':  'грэнфаа◆э',
  'grandmother':  'грэнма◆э',
  'weather':      'уэ◆э',

  // ── /w/ → «у» ────────────────────────────────────────────────────────────
  'wake up':      'уэйк ап',
  'walk':         'уок',
  'wallet':       'уолит',
  'want':         'уонт',
  'wardrobe':     'уордроуб',
  'wash':         'уош',
  'watch':        'уотч',
  'water':        'уотэ',
  'watermelon':   'уотэмелэн',
  'wednesday':    'уэнздэй',
  'white':        'уайт',
  'wife':         'уайф',
  'window':       'уиндоу',
  'wolf':         'уулф',
  'woman':        'ууман',
  'work':         'уёрк',
  'hallway':      'ҳолуэй',
  'subway':       'сабуэй',
  "you're welcome": 'йо уэлкам',
  'one':          'уан',
  'one hundred':  'уан ҳандрэд',
  'everyone':     'эвриуан',
  'between':      'битуин',
  'twelve':       'туэлв',
  'twenty':       'туэнти',
  'twins':        'туинз',
  'swim':         'суим',
  'sweater':      'суэтэ',
  'quality':      'куолити',
  'quantity':     'куонтити',
  'question':     'куэсчен',
  'language':     'лангуиҷ',

  // ── Номҳои хос ва вожаҳои байналмилалӣ: талаффузи АНГЛИСӢ, на русӣ ──────
  // Хонанда «адрес» мехонад ва дар Лондон ӯро намефаҳманд.
  'address':      'эдрэс',        // буд: «адрес» (русӣ)
  'passport':     'паасспот',     // буд: «паспорт» (русӣ)
  'manager':      'мэниҷэ',       // буд: «менеҷер» (русӣ)
  'engineer':     'энҷиниэ',      // буд: «инҷиниёр» (русӣ)
  'driver':       'драйвэ',       // буд: «драйвер» (русӣ)
  'supermarket':  'суупэмаакит',  // буд: «супермаркет» (русӣ)
  'america':      'эмэрикэ',      // буд: «америка»
  'tajikistan':   'тааҷикистаан', // буд: «тоҷикистон» (талаффузи тоҷикӣ)
  'tajik':        'тааҷиик',      // буд: «тоҷик»
  'dushanbe':     'душанбэй',     // буд: «душанбе»
  'london':       'ландэн',       // буд: «ландан» — қариб дуруст, ягона шуд
  'russia':       'рашэ',
  'england':      'инглэнд',      // ду шакл буд: «ингланд» ↔ «инглэнд»
};

const sql = connect();
const modules = await loadCourse(sql);
const ch = new Changes('03 — Талаффузи тоҷикӣ (th · w · номҳои хос)');
const seen = new Set();

for (const mod of modules) {
  for (const L of mod.lessons) {
    for (const w of L.words) {
      const key = w.word.trim().toLowerCase();
      const want = IPA_TJ[key];
      if (!want) continue;
      seen.add(key);
      ch.add({
        table: 'Word', id: w.id, field: 'ipaTajik',
        from: w.ipaTajik, to: th(want),
        where: `${loc(mod, L)} · «${w.word}»  ${w.ipa ?? ''}`,
      });
    }
  }
}

ch.print();

const unused = Object.keys(IPA_TJ).filter((k) => !seen.has(k));
if (unused.length) {
  console.log(`\n  ⚠️  ${unused.length} сатри ҷадвал калимаи мувофиқ наёфт:`);
  console.log('       ' + unused.join(', '));
}

console.log(`
  ⚙️  Шакли ҷории садои th:  /θ/ → «${TH_VOICELESS}»   /ð/ → «${TH_VOICED}»
     Барои иваз кардан танҳо ду доимии болои ин файлро тағйир диҳед.
     Ин ҚАРОРИ ТАЪЛИМӢ аст ва тасдиқи методистро мехоҳад.`);

ch.writeSql(new URL('./out/03-phonetics.sql', import.meta.url).pathname.replace(/^\//, ''));
await ch.apply(sql);
